---
import SiteLayout from "../layouts/SiteLayout.astro";
import "../styles/global.css";
import logo from "../assets/consortium.png";
import { theme } from "../config/theme.js";

const { background, text, accent, border, bandBackground, glowColor } = theme.colors;
---

<SiteLayout title="Sauvegarde Cloud Microsoft 365 – Consortium">
  <div style={`--bg:${background}; --text:${text}; --accent:${accent}; --border:${border}; --band:${bandBackground};`}>

    <style>
      /* ============ STYLE CONSORTIUM (bande blanche + cartes noires) ============ */
      .card-surface{
        background: rgb(18,18,18);
        border: 1px solid rgba(0,0,0,0.14);
        box-shadow: 0 14px 40px rgba(0,0,0,0.18);
      }
      .hover-tint{
        background: radial-gradient(circle at 18% 18%,
          color-mix(in srgb, var(--accent) 16%, transparent),
          transparent 62%);
      }
      .muted-on-dark{ color: color-mix(in srgb, white 70%, transparent); }
      .soft-on-dark { color: color-mix(in srgb, white 82%, transparent); }

      /* Texte sur la bande blanche */
      .band-title{ color: rgba(17,17,17,0.92); }
      .band-soft { color: rgba(17,17,17,0.76); }
      .band-muted{ color: rgba(17,17,17,0.62); }

      /* Hériter correctement quand on réutilise band-* à l’intérieur des cartes noires */
      .card-surface .band-title{ color: rgba(255,255,255,0.92); }
      .card-surface .band-soft { color: rgba(255,255,255,0.74); }
      .card-surface .band-muted{ color: rgba(255,255,255,0.62); }

      .pill{
        background: rgba(255,255,255,0.06);
        border: 1px solid rgba(255,255,255,0.10);
        color: rgba(255,255,255,0.92);
      }
      .pill:hover{
        border-color: color-mix(in srgb, var(--accent) 65%, transparent);
        filter: brightness(1.06);
      }
      .backbtn:hover{
        border-color: color-mix(in srgb, var(--accent) 65%, transparent);
        filter: brightness(1.06);
      }

      /* Petites icônes / SVG */
      svg[style*="--accent"]{ color: var(--accent) !important; }
    </style>

    <!-- HEADER SLOT -->
    <div slot="header" class="flex flex-col items-center">
      <img src={logo.src} alt="Consortium" class="h-32 md:h-36 w-auto" />
      <h1 class="mt-2 text-sm tracking-[0.35em] uppercase"
          style="color: color-mix(in srgb, var(--text) 60%, transparent);">
        Sauvegarde & Continuité
      </h1>
    </div>

    <!-- HERO SLOT -->
    <section slot="hero" class="relative mx-auto max-w-4xl px-4 text-center z-20 pt-2 pb-10">
      <div
        class="pointer-events-none absolute left-1/2 top-8 -translate-x-1/2 w-[70%] md:w-[55%] h-24 blur-[80px] opacity-70"
        style="background: color-mix(in srgb, var(--accent) 18%, transparent);"
      ></div>

      <h2 class="mt-5 text-4xl md:text-5xl font-semibold tracking-tight" style="color: var(--text);">
        Sauvegarde Cloud Microsoft 365
      </h2>

      <p class="mt-4 text-lg soft-on-dark">
        Une vraie sauvegarde pour Microsoft 365.
        <span class="block mt-2">
          Récupérez vos données en minutes — même après un incident.
        </span>
      </p>

      <div class="mt-6 flex flex-col sm:flex-row gap-3 justify-center">
        <a
          href="#activer"
          class="inline-flex items-center justify-center px-6 py-3 rounded-xl font-semibold transition hover:brightness-110"
          style="background: var(--accent); color: #111;"
        >
          Activer la sauvegarde
        </a>

        <a
          href="#pourquoi"
          class="inline-flex items-center justify-center px-6 py-3 rounded-xl font-semibold transition hover:brightness-110"
          style="background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.12); color: rgba(255,255,255,0.92);"
        >
          Pourquoi c’est essentiel
        </a>
      </div>

      <div class="mt-6 flex flex-wrap gap-2 justify-center">
        <span class="pill px-3 py-2 rounded-xl text-sm">Exchange</span>
        <span class="pill px-3 py-2 rounded-xl text-sm">OneDrive</span>
        <span class="pill px-3 py-2 rounded-xl text-sm">SharePoint</span>
        <span class="pill px-3 py-2 rounded-xl text-sm">Teams</span>
        <span class="pill px-3 py-2 rounded-xl text-sm">Données au Canada</span>
      </div>
    </section>

    <!-- FIXED BACK LINK -->
    <a
      href="/"
      class="backbtn fixed right-6 top-5 z-50 inline-flex items-center px-4 py-2 rounded-lg shadow-lg backdrop-blur transition-all"
      style="background: rgba(18,18,18,0.85); border: 1px solid rgba(255,255,255,0.10); color: color-mix(in srgb, white 90%, transparent);"
    >
      ← Retour à la page Services TI
    </a>

    <!-- BAND (BLANC) -->
    <section class="relative w-full overflow-hidden" style="background: var(--band);">
      <div class="h-[2px] w-full" style="background: var(--accent);"></div>
      <div class="h-14 w-full"
           style="background: linear-gradient(to bottom, color-mix(in srgb, var(--accent) 18%, transparent), transparent);">
      </div>

      <div class="absolute top-44 left-0 right-0 mx-auto w-[90%] h-44 blur-3xl z-0"
           style={`background-color: ${glowColor}; opacity: 0.22;`}>
      </div>

      <div class="relative z-10 mx-auto max-w-6xl px-4 pb-20 pt-10 space-y-12">
        <div id="pourquoi" class="text-center">
        <h3 class="band-muted text-xs uppercase tracking-[0.25em]">
          Pourquoi une sauvegarde indépendante?
        </h3>
        <p class="mt-4 text-2xl md:text-3xl font-semibold band-title">
          Parce que Microsoft 365 n’est pas une stratégie de restauration.
        </p>
        <!--<p class="mt-3 band-soft max-w-3xl mx-auto leading-relaxed">
          Microsoft offre des outils de rétention et de récupération, mais ils ont des limites.
          Une vraie sauvegarde vous donne une copie indépendante et une restauration rapide,
          même après une erreur, une suppression, une corruption ou une attaque.
        </p>-->
      </div>

      <!-- WHY GRID -->
      <section class="grid md:grid-cols-2 gap-10">
        <!-- Left: marketing + risks -->
        <div class="space-y-6">
          <div class="group relative overflow-hidden p-6 rounded-2xl card-surface">
            <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100 hover-tint"></div>
            <div class="relative">
            <p class="band-title text-xl leading-relaxed font-semibold">
              Microsoft 365 protège vos services…
              <span class="text-white">mais ce n’est pas une sauvegarde.</span>
            </p>
            <p class="mt-3 band-soft text-base leading-relaxed">
              Quand ça arrive, vous voulez une seule chose : <span class="band-title font-semibold">récupérer vite</span>,
              sans stress, sans perte, sans dépendre d’une “chance” de récupération.
            </p>
            <div
              class="mt-4 inline-flex items-center gap-2 px-3 py-2 rounded-xl pill text-sm"
            >
              <span class="text-white">✅</span>
              Restauration granulaire, rapide et indépendante
            </div>
            </div>
          </div>

          <div class="grid sm:grid-cols-2 gap-4">
            <div class="group relative overflow-hidden flex gap-3 p-4 rounded-xl card-surface">
              <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100 hover-tint"></div>
              <div class="relative flex gap-3 w-full">
              <span class="text-white text-xl">🗑️</span>
              <div>
                <p class="band-title font-semibold">Suppression accidentelle</p>
                <p class="band-soft text-sm">Fichiers, courriels, dossiers supprimés trop vite.</p>
              </div>
              </div>
            </div>

            <div class="group relative overflow-hidden flex gap-3 p-4 rounded-xl card-surface">
              <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100 hover-tint"></div>
              <div class="relative flex gap-3 w-full">
              <span class="text-red-400 text-xl">🦠</span>
              <div>
                <p class="band-title font-semibold">Ransomware & compromission</p>
                <p class="band-soft text-sm">OneDrive / SharePoint / Exchange sont des cibles.</p>
              </div>
              </div>
            </div>

            <div class="group relative overflow-hidden flex gap-3 p-4 rounded-xl card-surface">
              <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100 hover-tint"></div>
              <div class="relative flex gap-3 w-full">
              <span class="text-purple-400 text-xl">📄</span>
              <div>
                <p class="band-title font-semibold">Fichiers corrompus</p>
                <p class="band-soft text-sm">Altération silencieuse, sync, mauvaises versions.</p>
              </div>
              </div>
            </div>

            <div class="group relative overflow-hidden flex gap-3 p-4 rounded-xl card-surface">
              <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100 hover-tint"></div>
              <div class="relative flex gap-3 w-full">
              <span class="text-indigo-400 text-xl">⏱️</span>
              <div>
                <p class="band-title font-semibold">Récupération limitée</p>
                <p class="band-soft text-sm">Rétention, délais, politiques: pas illimité.</p>
              </div>
              </div>
            </div>

            <div class="group relative overflow-hidden flex gap-3 p-4 rounded-xl card-surface sm:col-span-2">
              <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100 hover-tint"></div>
              <div class="relative flex gap-3 w-full">
              <span class="text-yellow-400 text-xl">👤</span>
              <div>
                <p class="band-title font-semibold">Départ d’employés</p>
                <p class="band-soft text-sm">Comptes supprimés = courriels & fichiers perdus.</p>
              </div>
              </div>
            </div>
          </div>

          <a
            href="#activer"
            class="inline-flex items-center gap-2 text-sm font-semibold band-title hover:opacity-80 transition"
          >
            Voir la tarification et activer la sauvegarde →
          </a>
        </div>

        <!-- Right: proof / benefits cards -->
        <div class="grid grid-cols-1 gap-6">
          <div
            class="p-6 rounded-2xl card-surface"
          >
            <p class="band-title text-lg font-semibold">Ce que vous gagnez</p>
            <p class="mt-2 band-soft leading-relaxed">
              Une assurance opérationnelle : retrouver rapidement ce qui compte, sans pertes ni interruptions prolongées.
            </p>

            <div class="mt-4 space-y-3">
              <div class="flex items-start gap-3">
                <span class="text-white text-lg">⚡</span>
                <p class="band-soft">
                  <span class="font-semibold">Restauration en minutes</span> (courriel, fichier, dossier, bibliothèque)
                </p>
              </div>
              <div class="flex items-start gap-3">
                <span class="text-indigo-400 text-lg">🧩</span>
                <p class="band-soft">
                  <span class="font-semibold">Granulaire</span> : pas besoin de tout restaurer pour récupérer un élément
                </p>
              </div>
              <div class="flex items-start gap-3">
                <span class="text-purple-400 text-lg">🛡️</span>
                <p class="band-soft">
                  <span class="font-semibold">Indépendante</span> : votre copie reste accessible même après incident
                </p>
              </div>
            </div>
          </div>

          <div
            class="flex items-start gap-4 p-6 rounded-2xl
                   card-surface"
          >
            <!-- Shield Check -->
            <svg
              class="w-10 h-10 text-white"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              viewBox="0 0 24 24"
             style="color: var(--accent);">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6.75l7.5 3.75-7.5 3.75L4.5 10.5l7.5-3.75z"
              />
            </svg>
            <div>
              <p class="band-title text-lg font-semibold">Protection contre les incidents modernes</p>
              <p class="band-soft mt-1 leading-relaxed">
                La sauvegarde est votre plan de reprise : simple, rapide, et conçu pour récupérer même quand tout va mal.
              </p>
            </div>
          </div>

          <div
            class="flex items-start gap-4 p-6 rounded-2xl
                   card-surface"
          >
            <!-- Document-text -->
            <svg
              class="w-10 h-10 text-indigo-400"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              viewBox="0 0 24 24"
             style="color: var(--accent);">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M9 12h6M9 9h6m-6 6h6m5 2.25V6.75A2.25 2.25 0 0017.25 4.5H6.75A2.25 2.25 0 004.5 6.75v10.5A2.25 2.25 0 006.75 19.5h10.5A2.25 2.25 0 0019.5 17.25z"
              />
            </svg>
            <div>
              <p class="band-title text-lg font-semibold">Restauration “sans casse-tête”</p>
              <p class="band-soft mt-1 leading-relaxed">
                Retrouver un mail, un document ou une version précise — rapidement — sans perturber le reste.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- HOW IT WORKS -->
      <section class="mt-14 space-y-8">
        <h4 class="text-2xl font-semibold band-title mb-6">
          Comment ça fonctionne
        </h4>

        <div class="grid md:grid-cols-3 gap-6">
          <div class="rounded-2xl p-6 card-surface">
            <p class="band-title font-semibold text-lg">1) Connexion</p>
            <p class="band-soft mt-2 text-sm leading-relaxed">
              On connecte la solution à votre tenant Microsoft 365 (accès sécurisé, configuration guidée).
            </p>
          </div>

          <div class="rounded-2xl p-6 card-surface">
            <p class="band-title font-semibold text-lg">2) Sauvegardes automatiques</p>
            <p class="band-soft mt-2 text-sm leading-relaxed">
              Des sauvegardes régulières protègent Exchange, OneDrive, SharePoint et Teams.
            </p>
          </div>

          <div class="rounded-2xl p-6 card-surface">
            <p class="band-title font-semibold text-lg">3) Restauration à la demande</p>
            <p class="band-soft mt-2 text-sm leading-relaxed">
              Besoin de récupérer? On restaure rapidement (fichier, mail, dossier, etc.).
            </p>
          </div>
        </div>
      </section>

      <!-- COMPLIANCE NOTE -->
      <section class="mt-14">
        <div class="rounded-2xl p-6" style="background: rgba(0,0,0,0.04); border: 1px solid rgba(0,0,0,0.08);">
          <div class="flex items-start gap-4">
          <svg
            class="w-8 h-8 flex-shrink-0"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            viewBox="0 0 24 24"
           style="color: var(--accent);">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M12 6.75l7.5 3.75-7.5 3.75L4.5 10.5l7.5-3.75z"
            />
          </svg>

          <div>
            <h4 class="text-xl font-semibold mb-2 band-title">
              Conformité & continuité (Loi 25)
            </h4>
            <p class="leading-relaxed band-soft">
              La Loi 25 renforce les obligations de protection des renseignements personnels.
              Une sauvegarde indépendante aide à assurer la continuité et la restauration
              après un incident, tout en réduisant l’impact opérationnel.
            </p>
          </div>
          </div>
        </div>
      </section>

      <!-- INCLUDED -->
      <section class="mt-14 space-y-8">
        <h4 class="text-2xl font-semibold band-title mb-6">
          Ce qui est inclus
        </h4>

        <div class="grid md:grid-cols-3 gap-6">
          <div class="rounded-2xl p-6 card-surface">
            <svg
              class="w-8 h-8 text-white mb-3"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              viewBox="0 0 24 24"
             style="color: var(--accent);">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 16.5v-9m0 9l3-3m-3 3l-3-3m11.25-3a4.5 4.5 0 00-4.5-4.5h-1.28a6 6 0 10-5.47 9"
              />
            </svg>
            <h5 class="font-semibold text-lg mb-2 text-white">
              Sauvegarde automatique
            </h5>
            <p class="band-soft text-sm">
              Exchange, OneDrive, SharePoint et Teams protégés sans intervention.
            </p>
          </div>

          <div class="rounded-2xl p-6 card-surface">
            <svg
              class="w-8 h-8 text-indigo-400 mb-3"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              viewBox="0 0 24 24"
             style="color: var(--accent);">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M16.5 9.75l-4.5 4.5-4.5-4.5"
              />
            </svg>
            <h5 class="font-semibold text-lg mb-2 text-indigo-300">
              Restauration rapide
            </h5>
            <p class="band-soft text-sm">
              Récupération granulaire : fichier, courriel, dossier ou bibliothèque.
            </p>
          </div>

          <div class="rounded-2xl p-6 card-surface">
            <svg
              class="w-8 h-8 text-purple-400 mb-3"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              viewBox="0 0 24 24"
             style="color: var(--accent);">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M3.75 7.5h16.5M3.75 12h16.5M3.75 16.5h16.5"
              />
            </svg>
            <h5 class="font-semibold text-lg mb-2 text-purple-300">
              Hébergement au Canada
            </h5>
            <p class="band-soft text-sm">
              Une approche alignée avec les besoins de confidentialité et de conformité.
            </p>
          </div>
        </div>

        <h4 class="text-2xl font-semibold band-title mb-3">
          Pourquoi Datto
        </h4>
        <p class="leading-relaxed band-soft">
          Datto est une plateforme reconnue pour la robustesse de ses sauvegardes
          et la qualité de ses restaurations. Avec les services TI des EÉSAD, vous obtenez une implantation
          guidée, un accompagnement humain, et une solution simple à opérer au quotidien.
        </p>

        <div class="mt-6 grid md:grid-cols-2 gap-4">
          <div class="p-5 rounded-2xl bg-slate-900/25 ">
            <p class="band-title font-semibold">✅ Implantation incluse</p>
            <p class="band-soft text-sm mt-1">Configuration initiale et validation de la sauvegarde.</p>
          </div>
          <div class="p-5 rounded-2xl bg-slate-900/25 ">
            <p class="band-title font-semibold">✅ Support & accompagnement</p>
            <p class="band-soft text-sm mt-1">Vous n’êtes pas seul lors d’un incident.</p>
          </div>
        </div>

        <h4 class="text-2xl font-semibold band-title">
          Questions fréquentes
        </h4>

        <div class="grid md:grid-cols-2 gap-6">
          <div class="rounded-2xl p-6 card-surface ">
            <p class="band-title font-semibold">“On a déjà OneDrive / SharePoint, c’est sécurisé non?”</p>
            <p class="band-soft text-sm mt-2 leading-relaxed">
              Sécurisé ≠ sauvegardé. La sync propage aussi les erreurs (suppression, corruption).
              La sauvegarde indépendante sert à revenir en arrière rapidement.
            </p>
          </div>

          <div class="rounded-2xl p-6 card-surface ">
            <p class="band-title font-semibold">“Est-ce que ça protège Teams?”</p>
            <p class="band-soft text-sm mt-2 leading-relaxed">
              Oui : les données sous-jacentes (SharePoint/OneDrive/Exchange) qui alimentent Teams
              sont incluses dans la protection.
            </p>
          </div>

          <div class="rounded-2xl p-6 card-surface ">
            <p class="band-title font-semibold">“Combien de temps pour restaurer?”</p>
            <p class="band-soft text-sm mt-2 leading-relaxed">
              Ça dépend du volume, mais l’objectif est la restauration rapide et granulaire
              (souvent minutes plutôt qu’heures/jours).
            </p>
          </div>

          <div class="rounded-2xl p-6 card-surface ">
            <p class="band-title font-semibold">“Pourquoi hébergement au Canada?”</p>
            <p class="band-soft text-sm mt-2 leading-relaxed">
              Pour mieux répondre aux attentes de confidentialité et aux réalités de conformité
              (notamment au Québec).
            </p>
          </div>
        </div>
      </section>

        <div id="activer"></div>

        <div
          class="rounded-2xl p-10 card-surface relative overflow-hidden" style="background: linear-gradient(135deg, color-mix(in srgb, var(--accent) 62%, rgb(18,18,18)), rgb(18,18,18)); border: 1px solid rgba(255,255,255,0.10);"
        >
          <div class="pointer-events-none absolute inset-0 hover-tint"></div>
          <h4 class="text-3xl font-semibold mb-2">Tarification simple</h4>
          <p class="text-white/90 text-lg mb-6">
            Activez une sauvegarde indépendante pour Microsoft 365, sans complexité.
          </p>

          <p class="text-xl mb-6">
            <span class="font-extrabold text-5xl">$3</span>
            <span class="text-white/90"> / utilisateur / mois</span>
          </p>

          <ul class="list-disc ml-6 space-y-2 text-lg mb-8">
            <li>Configuration initiale incluse</li>
            <li>Engagement 36 mois</li>
            <li>Maintenance mensuelle incluse</li>
            <li>Protection Exchange, OneDrive, SharePoint et Teams</li>
          </ul>

          <div class="flex flex-col sm:flex-row gap-3">
            <button
              id="openFormBtn"
              class="inline-flex justify-center items-center mt-2 px-8 py-3 rounded-xl font-semibold
                     bg-white text-black hover:bg-slate-100 transition"
            >
              Activer la sauvegarde
            </button>

            <a
              href="/"
              class="inline-flex justify-center items-center mt-2 px-8 py-3 rounded-xl font-semibold
                     bg-white/10 border border-white/20 text-white
                     hover:bg-white/20 transition"
            >
              Parler à un conseiller
            </a>
          </div>

          <!-- HIDDEN FORM -->
          <div
            id="activationFormContainer"
            class="overflow-hidden max-h-0 opacity-0 transition-all duration-500 mt-6"
          >
            <form
              id="activationForm"
              class="space-y-4 bg-white/10 backdrop-blur p-6 rounded-xl border border-white/20"
            >
              <h5 class="text-xl font-semibold mb-2">
                Demande d’activation
              </h5>

              <label class="block text-sm font-medium">
                Nom de l’organisation
              </label>
              <input
                type="text"
                name="activation_org"
                required
                class="w-full rounded-lg px-3 py-2 bg-white/20 border border-white/30
                       placeholder-white/40 text-white focus:ring-2 focus:ring-white"
              />

              <label class="block text-sm font-medium">
                Courriel
              </label>
              <input
                type="email"
                name="activation_email"
                required
                class="w-full rounded-lg px-3 py-2 bg-white/20 border border-white/30
                       placeholder-white/40 text-white focus:ring-2 focus:ring-white"
              />

              <label class="block text-sm font-medium">
                Nombre d’utilisateurs
              </label>
              <input
                type="number"
                name="activation_users"
                required
                class="w-full rounded-lg px-3 py-2 bg-white/20 border border-white/30
                       placeholder-white/40 text-white focus:ring-2 focus:ring-white"
              />

              <button
                type="submit"
                class="mt-4 w-full px-4 py-2 bg-white text-black rounded-lg font-semibold
                       hover:bg-slate-100 transition"
              >
                Envoyer la demande
              </button>

              <button
                type="button"
                id="closeFormBtn"
                class="mt-2 w-full px-4 py-2 bg-white/10 text-white rounded-lg font-medium
                       border border-white/20 hover:bg-white/20 transition"
              >
                Fermer
              </button>

              <p id="activationStatus" class="text-sm text-white/80 mt-2"></p>
            </form>
          </div>
        </div>
      </div>
    </section>

    <script>
  const btn = document.getElementById("openFormBtn");
  const closeBtn = document.getElementById("closeFormBtn");
  const panel = document.getElementById("activationFormContainer");
  const form = document.getElementById("activationForm");
  const status = document.getElementById("activationStatus");

  if (btn && closeBtn && panel && form && status) {
    // OPEN PANEL
    btn.addEventListener("click", () => {
      panel.style.maxHeight = panel.scrollHeight + "px";
      panel.style.opacity = "1";
      panel.scrollIntoView({ behavior: "smooth", block: "start" });
    });

    // CLOSE PANEL
    closeBtn.addEventListener("click", () => {
      panel.style.maxHeight = "0";
      panel.style.opacity = "0";
    });

    // FORM SUBMISSION
    form.addEventListener("submit", async (e) => {
      e.preventDefault();

      const fd = new FormData(form);
      const data = {};
      for (const [k, v] of fd.entries()) data[k] = v;

      status.textContent = "Envoi…";

      try {
        const res = await fetch("/api/submit", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(data),
        });

        if (res.ok) {
          status.textContent = "Demande envoyée! On vous contacte sous peu.";
          form.reset();
        } else {
          status.textContent = "Erreur d’envoi. Réessayez ou contactez-nous.";
        }
      } catch (err) {
        status.textContent = "Erreur réseau. Réessayez plus tard.";
      }
    });
  }
</script>

  </div>
</SiteLayout>
